include "class.robray"
include "table.robray"


frame holder_jar {
    parent end_effector_grasp_jar;
    rpy[-90*deg, 0, 0];
    translation[0, 0, .05];

    geometry {
        isa holder;
    }
}

frame pickle_jar {
     parent holder_jar;
     //translation [0, 0, 0]; //comment for grabbing lid
     //rpy[0, -90*deg, 0];
     //translation [0.2, 0.2, 0.1];
     //translation [-0.2, 0.1, 0]; //DO NOT DELETE NO MATTER WHAT. LEFT ARM WORKS
     
     rpy[90*deg, 0, 0];
     translation[0,0.045, 0];
     
     geometry {
         isa jar;
     }
 }

 frame pickle_lid {
     parent pickle_jar;
     //translation [-0.2, -0.1, 0]; //comment for grabbing lid
     //translation [0, 0, 0]; //comment for grabbing jar
     translation[0, 0, 0.1];
     
     geometry {
         isa lid;
     }
 }